//! dwl-specific modules (consume the [`WindowManager`](wafflebar_core::WindowManager) seam via
//! [`WmEvent`](wafflebar_core::WmEvent)s, never dwl APIs directly). M2: `tags`, `window`,
//! `taskbar`.

pub mod tags;
